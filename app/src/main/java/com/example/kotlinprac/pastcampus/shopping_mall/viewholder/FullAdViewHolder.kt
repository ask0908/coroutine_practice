package com.example.kotlinprac.pastcampus.shopping_mall.viewholder

import com.example.kotlinprac.databinding.ItemFullAdBinding

/**
 * 이 클래스는 이대로 완결이다. XML에서 아이템을 넘겨받고 XML 안에서 처리하기 때문
 */
class FullAdViewHolder(
    val binding: ItemFullAdBinding
): BindingViewHolder<ItemFullAdBinding>(binding)